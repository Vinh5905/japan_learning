DATABASE_URL="postgresql://kanji:kanji@localhost:5432/kanji_learning?schema=public"

# AnkiConnect endpoint. Defaults to http://127.0.0.1:8765 — the AnkiConnect
# add-on running inside the user's open Anki desktop app. Override only when
# Anki is reachable on a different host/port.
ANKI_CONNECT_URL="http://127.0.0.1:8765"
